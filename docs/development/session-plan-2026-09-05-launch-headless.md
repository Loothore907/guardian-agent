# Launch and headless-session integration plan

The user approved continuing all in-scope actions on 2026-09-05. This slice connects
confirmed session authority to launch and prepares the unattended judge execution
path. It preserves existing work and credentials and adds no third-party dependency.

1. Include exact GitHub targets and action/mutation limits in the initial preview
   digest. Reject intent outside mission permissions. Store the bound grant before
   worker execution; interrupt launch if grant activation fails.
2. Execute the competition journey using that stored grant without a later exact
   approval prompt. Preserve the legacy exact-approval entry point and all broker
   membership, resource, risk and expiry checks.
3. Add a trusted headless entry point whose scope is fixed by deployment config,
   with authenticated ingress as the session trigger. Public objectives and request
   data cannot select credentials, authority, targets or deployment settings.
4. Verify noninteractive credential resolution and renewal failure behavior using
   synthetic provider/SecretStash adapters, and document remaining provisioning.
5. Run focused and complete Windows/Linux checks, update the handoff and claims,
   and prepare remaining deployment requirements as concrete reviewable scope.

No real provider effect, paid provisioning, host update, remote merge, credential
migration, commit, push or deployment is included. Existing hosting decisions are
retained; new external changes need a bounded resource/ref/action plan.

## Local closeout

The enumerated local slice is complete. [Evidence](evidence/2026-09-05-launch-headless.md)
records Windows/Linux gates and runtime probes. [Deployment preparation](headless-judge-setup.md)
names the remaining external work; no hosted readiness or WSL repair is claimed.
