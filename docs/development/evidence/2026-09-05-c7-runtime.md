# C7 local runtime validation, 2026-09-05

Scope: [approved session plan](../session-plan-2026-09-05-c7-runtime-integration.md),
[ADR-0051](../../adr/0051-bounded-worker-portal-runtime.md).
Baseline: `codex/13-c6-linux-provider-containment`, `e5b1217`; accumulated source
remains uncommitted. Initial modified/untracked file hashes are in ignored
`tmp/c7-runtime/baseline.json`. No real credentials, paid providers, remote fixtures,
commits, pushes or deployments were used in this slice.

## Reproducible evidence map

| Boundary                         | Evidence                                                                                                                                                                                                                                                                                                                                                                                      |
| -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Exact sanitized external results | `packages/worker/src/external-results.test.ts`: digest mutation, session/grant binding, unsafe text and foreign research provenance; UUID property cases                                                                                                                                                                                                                                      |
| Bounded PR content               | `packages/adapter-github/src/review.test.ts`: fixed endpoints, seed and changed-file content, head/base/body changes, unsafe/oversized text, incomplete patches                                                                                                                                                                                                                               |
| Durable replay and accounting    | `packages/authority-store/src/index.test.ts`: external claim does not double charge; replay rejection survives reopening; existing migration/expiry/audit corpus                                                                                                                                                                                                                              |
| Continuation                     | `apps/reference-supervisor/src/bootstrap.test.ts`: eligible denial, retained history, exact subsequent turn, terminal turn limit and correctly hashed foreign-session results                                                                                                                                                                                                                 |
| Deadline                         | `apps/reference-supervisor/src/worker-deadline.test.ts`: in-flight abort/owner interruption and rejection before expired startup                                                                                                                                                                                                                                                              |
| Supervisor scope                 | `apps/reference-supervisor/src/judge-runtime-scope.test.ts`: exact targets, connection, head/base and volume; no implicit local tools or GitHub grant for research                                                                                                                                                                                                                            |
| Portal confirmation              | `apps/reference-supervisor/src/judge-portal-runtime.test.ts`: actual bootstrap behind synthetic launcher, no worker before confirmation, mismatched preview rejection and one-use cleanup                                                                                                                                                                                                     |
| Actual service children          | `apps/reference-supervisor/src/c7-service-children.test.ts`: separate authority/worker/research/Guardian/broker children, grant before worker, one controlled release retrieval, injected outside-domain request, pre-dispatch classified denial, useful cited completion, contiguous audit chain and durable terminal state; GitHub read/merge near misses remain; synthetic transports only |
| Model-facing projection          | `apps/worker-service/src/nebius.test.ts`: prior/current denial history exposes only the allowlisted cause/stage plus public code/disposition and omits trusted policy/authorization details; provider metering, malformed output and credential redaction regressions                                                                                                                         |
| Existing portal/host controls    | Portal route, preview/replay/expiry, budget admission/settlement, persistent pool and headless-host suites remain required                                                                                                                                                                                                                                                                    |

The child tests use a synthetic worker and fixed in-memory provider transports.
They prove runtime composition and content delivery; they do not prove that a real
model takes the bait. The portal tests use a synthetic launcher and budget inputs;
they do not establish hosted isolation or live budget-service readiness.
The September 9 extension is recorded separately in
[`2026-09-09-local-denial-recovery.md`](2026-09-09-local-denial-recovery.md); the
gate counts below remain the historical September 5 results.

Windows service-child tests use a shared fixture clock in the parent and child
entrypoints. The initial full Windows run reproduced strict future-time rejection
in real-clock children (three failures); the fixed-clock tests do not close that C6
diagnosis or relax production timestamps. Linux uses actual clocks. Only the C7
test entrypoints import `scripts/test-fixtures/c7-clock.mjs`.

The Linux stage was verified against initial/current source hashes before each
synchronization: 334 source/config/test files, normalized for CRLF. Historical
stage documentation differed and was preserved rather than overwritten. Manifests
and sync verification scripts remain in ignored `tmp/c7-runtime/`. WSL repeatedly
reported a systemd user-session startup warning. An earlier successful command lost
its `/tmp` log across the WSL lifecycle; final logs use the persistent stage directory.

## Gate results

| Command / environment                                                          | Final result                                                                                                                                        | Retained local log under `tmp/c7-runtime/` |
| ------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| Windows `pnpm check`                                                           | Passed; 648 Vitest tests, 18 skipped; SQLite 7/1 skipped; reset 2; preflight 6; supervised GitHub 5; headless host 1; format/lint/type/build passed | `windows-check.log`                        |
| Linux `pnpm check`                                                             | Passed; 660 Vitest tests, 6 skipped; SQLite 8; reset 2; preflight 6; supervised GitHub 5; headless host 1; format/lint/type/build passed            | `c7-linux-check.log`                       |
| Linux `pnpm test:linux-platform`                                               | 2 passed; current-user-only IPC/SQLite and broad-permission/symlink rejection                                                                       | `c7-linux-platform.log`                    |
| Windows/WSL `pnpm test:reference-runtime`                                      | 1 passed; production executor isolation and embedded bypass probes, about 20.3 seconds                                                              | `reference-runtime.log`                    |
| Windows/WSL `pnpm test:session-enforcement`                                    | 4 passed; alternate-tool rejection, unavailable research credentials fail closed, direct-Git/network/filesystem isolation                           | `session-enforcement.log`                  |
| `pnpm audit --prod --audit-level high --fetch-retries 0 --fetch-timeout 15000` | Passed after allowing registry access; no known vulnerabilities found                                                                               | `audit.log`                                |
| Dependency boundaries (both complete checks)                                   | No violations: 233 modules, 530 dependencies                                                                                                        | Complete-check logs                        |
| `git diff --check`                                                             | Passed; existing CRLF normalization notices only                                                                                                    | Terminal result                            |

After the full checks, the existing fixture tests were strengthened to exercise
piloted `reserveTarget` against a changed head/PR, concurrent seeded reservation
and restart reuse. Both fixture tests passed on Windows and Linux; their focused
format/lint check passed. No production code changed after the full checks.

Linux verification used `/home/loothore907/guardian-w27-session-20260904` with the
existing Node 24.19.0 runtime and `/usr/sbin` available for reference tools. Live
provider suites remain opt-in/skipped. Logs and baseline manifests are ignored local
artifacts; the named tests above are the reproducible, reviewable evidence.

All six local plan steps are complete. Protected/live/hosted acceptance in the
separate external gate remains open. No commit, source publication or deployment
was performed, and the existing accumulated worktree remains intact.

## C6/C7 gate disposition

- Local typed dispatch, exact result binding, continuation and portal composition:
  implementation present; acceptance is bounded to the tests listed above.
- Provider credentials remain in the existing services; no new credential owner,
  arbitrary transport, production dependency or authority role is introduced.
- Existing platform/runtime probes passed for their documented filesystem,
  direct-network, alternate-tool, credential-path and Git-push bypass scope; these
  results do not establish the broader intended-host provider containment corpus.
- Intended-host provider-process/credential/artifact containment corpus and retained
  protected audit evidence remain open C6 gates. Synthetic children do not close them.
- WSL warm-start/systemd reliability and Windows real-clock diagnosis retain their
  separate dispositions. Offline tests do not prove a permanent host repair.
- Real Nebius/Tavily/GitHub observations, exact external resources/fixtures, hosted
  cold boot, repeated unattended runs, current remote CI/review and live enablement
  remain pending separate bounded authority. No assurance promotion is claimed.
